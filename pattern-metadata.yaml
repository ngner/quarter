name: quarter
displayName: Quarter (ACM tenancy on Multicloud GitOps)
description: >-
  Distinct Validated Pattern for hub GitOps: Multicloud GitOps baseline (operators,
  clustergroup) plus ACM tenancy policy repos and tenant console. Lineage: hub layout
  and values track validatedpatterns/multicloud-gitops at the pin recorded in deps/mcg-ref.txt
  (see README). Argo CD Applications are reconciled in the pattern-scoped namespace
  quarter-hub (not openshift-gitops).
tier: sandbox
support: community-best-effort
